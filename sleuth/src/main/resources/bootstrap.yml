server:
  port: 24001

spring:
  profiles:
    active: eureka
  application:
    name: sleuth

logging:
  level:
    org:
      springframework:
        web:
          servlet:
            DispatcherServlet: DEBUG